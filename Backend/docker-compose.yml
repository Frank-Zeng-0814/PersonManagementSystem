services:
  backend:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - ./Backend/.env
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - PORT=8080
    restart: unless-stopped
